# 【20170806课后练习】
# 模拟微信朋友圈的服务端，需要满足以下场景：
# 1.发表内容时带上图片信息，内容包括（具体内容和id，id指定全局唯一标识），客户端请求后服务端将信息保存在excel中
# 2.删除无效的内容
# 3.对朋友圈进行查询操作，返回所有的查询结果（内容在控制台进行打印，文件保存在临时目录）
# 注意：客户端：使用时建议做一个流程，即发表->查询->删除，这样来测试服务端
#       测试异常：如果进行查询或者删除不存在的资源时，提示资源不存在并且返回错误码为404
# 题目：有一个已经排好序的数组。现输入一个数，要求按原来的规律将它插入数组中。

# 题目：有n个整数组成的一个列表，写一个函数，
# 使其前面各数顺序向后移m个位置，最后m个数变成最前面的m个数

# 题目:有两个磁盘文件A和B,各存放一行字母,要求把这两个文件中的信息合并
# (按字母顺序排列), 输出到一个新文件C中.

# 习题：通过进程池的方式。在一个目录下，创建500个文件，并且文件名都是顺序递增的，
# 例如1.txt，2.txt，在文件里面，写上当前的时间。

# 题目：一球从100米高度自由落下，每次落地后反跳回原高度的一半；再落下，求它在
# 　　　第10次落地时，共经过多少米？第10次反弹多高？

# 题目：从键盘输入一些字符，逐个把它们送到磁盘上去，直到输入字符包含#号为止。

# 把昨天习题中，大家自己写的日志系统，关于函数的进出部分，使用装饰器重写。
# 没上课的同学，可参考我发共享的log.rar

# 题目：猴子吃桃问题：猴子第一天摘下若干个桃子，当即吃了一半，还不瘾，又多吃了一个
# 　　　第二天早上又将剩下的桃子吃掉一半，又多吃了一个。以后每天早上都吃了前一天剩下
# 　　　的一半零一个。到第10天早上想再吃时，见只剩下一个桃子了。求第一天共摘了多少。

 每日一练：1、使用多线程交替打印数字
#      要求：
#       1）线程1只打印奇数，线程2只打印偶数
#       2）要求打印1、2、3、4、5、6、7……


# 每日一练：使用生产者方式实现多线程写队列和读队列
# 要求：边读边写

# 每日一练：
# 3、实现多线程入队列，然后按规则出队列
#       要求：
#       1）实现一个线程不断生成一个随机数到一个队列中(考虑使用Queue    这个模块)
#       2）实现一个线程从上面的队列里面不断的取出奇数
#       3）实现另外一个线程从上面的队列里面不断取出偶数

# 每日一练：猴子第一天摘下若干个桃子，当即吃了一半，还不过瘾，又多吃了一个。
# 第二天早上又将剩下的桃子吃掉一半，又多吃了一个。
# 以后每天早上都吃了前一天剩下的一半零一个。到第10天早上想吃时，
# 只剩下一个桃子了。求第一天共摘多少个桃子

 当前目录下有三个班的分数文件，名称分别为score1.txt、score2.txt、score3.txt
# 的文本文件，存放着三个班学生的计
# 算机课成绩，共有学号、总评成绩两列。用多线程编程的方式，
# 请查找班级最高分和最低分的学生，以及年级最高分和最低分的学生，
# 并在屏幕上显示其学号和成绩。

# 《JAVA每日一练》有三个孪生兄弟，他们长相相同(脸，眼睛，鼻子，嘴巴，耳朵)，
# 但是音色不同，三个人都喜欢打篮球，打篮球时老大喜欢打前锋，老二喜欢打中锋，
# 老三喜欢打后卫，各自也有不同吃的喜好，老大喜欢吃肉青菜，老二喜欢吃牛肉，
# 老三喜欢吃鸡肉，他们都喜欢学习，请用类的继承与接口实现这些描述
# 【Python每日一练】读取一个目录，将文件名称转换成json格式，
# 要求每个目录下必须包括文件和文件夹，输出至少3层目录的结果，
# 并将结果保存在一个文件中，例如{"test1": {"test11": {}, "test12.txt": "test12.txt", "test12": {}, "test11.txt": "test11.txt"}, "test1.txt": "test1.txt", "test2": {"test22.txt": "test22.txt", "test21": {}, "test22": {}, "test21.txt": "test21.txt"}, "test2.txt": "test2.txt"}
# 【Python每日一练】在上一题生成的json格式目录的基础上，
# 从文件中读取结果转换成json格式，要求对json进行遍历，
# 查找一个存在的文件名，对文件名进行修改，要求将所有的文件名修改成指定的文件名，
# 比如将目录中所有的test修改成testupdate



# 输入一个字符和行号，将输入的字符加入到对应行的后面，
# 需要以逗号隔开，如果该行第四个位置已经存在数据则进行修改操作，
# 比如key3,value3,value3,value3,再次输入value4和3后，
# 更新对应的值为key3,value3,value3,value4,始终保证每一行只有四个数据，
# 这里不考虑输入包括逗号的情况

# 【Python每日一练】小明去水果店买水果，需要买桃子(比如3块钱一斤)，
# 西瓜（比如2.5块钱一斤），葡萄（比如15块钱一斤），请编写一个程序计算
# 小明最终需要付多少钱，要求使用面向对象思想编程，，编写一个水果类Fruit，
# 桃子、西瓜和葡萄分别继承水果类，定义一个测试类，在测试类中调用其它类，
# 输入小明买的水果类型和重量(建议用字典表示)得出总共需要付多少钱


# 【Python每日一练】小明同学今天还是去家里附近的水果店买水果，
# 称完水果后发现忘记带钱了，然后回家拿钱，拿到钱后付完钱就匆忙走了，
# 到家后突然发现老板没找钱给他，他又跑回去，然后顺利到家，糟糕的一次购物。
# 就小明付钱这个动作使用装饰器的方式实现（回家拿钱->付钱->找钱）
# 大家回顾下装饰器


# @全体成员 【python每日一练】excel中保存了一些用户信息包括（编号,用户名，性别，
# 联系方式等），要求从excel中读取信息并换成json格式的数据


# 【python每日一练】使用flask实现后端服务，要求编写一个test方法，
# 同时支持get/put/delete/post类型的请求，
# 客户端使用requests分别发送这四类请求，并打印返回结果


# @全体成员 【python每日一练】有一个数据库配置文件，文件的内容为
# #ip地址
# hostIP=127.0.0.1
# #数据库类型
# dbType=mysql
# #数据库用户名
# userName=test
# #数据库密码
# password=test
# #数据库端口
# port=3306
# 请从文件中读取配置信息，并转换成json格式进行输出

# 【python每日一练】在使用脚本自动安装环境完成后，
# 有些服务安装完成后需要手工修改配置文件信息，
# 请使用python实现配置文件信息修改，比如tomcat
# 中需要修改服务中数据库链接信息，要求修改以下信息，
# 将ip地址(127.0.0.1)，用户名（root）、密码（root）进行修改，
# 分别修改成192.168.1.2，system,password.修改的值也可以自己指定，
# 最终结果保证文件修改后格式不能变化。
# jdbc.url=jdbc:mysql://127.0.0.1:3306/hiip?useUnicode=true&characterEncoding=utf-8
# jdbc.username=root
# jdbc.password=root
# hibernate.dialect=org.hibernate.dialect.MySQLDialect

# 【python每日一练】定义一个Person类，基本属性包括pName,pSex,
# pAge和pAddress,pAddress又包括家庭地址(homeAddress)，
# 公司地址(companyAdrress)，常用地址(frequentAdress)，
# 地址请使用类来定义。即地址对象为Person类的一个属性。
# 最后实例化3个Persion对象，
# 并将结果转化成功json格式输出


















































